<h1>Виды работ</h1>

<%= link_to "Добавить новый вид работ", new_work_type_path %>

<% @work_types.each do |work_type| %>
	<h1><%= work_type.id %></h1>
	Название: <%= work_type.name %>
	<br>	
	Измеряется в: <%= work_type.dimension %>
	<br>
	<p>------------------------------------------------------------</p>
<% end %>

